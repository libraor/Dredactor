[project]
name = "dredactor"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.1.0",
    "pyyaml>=6.0",
    "streamlit>=1.28.0",
    "typer[all]>=0.12.0",
]

[project.optional-dependencies]
dev = [
    "black>=23.0.0",
    "mypy>=1.5.0",
    "pylint>=3.0.0",
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
]
